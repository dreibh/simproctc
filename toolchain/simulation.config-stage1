#!/bin/sh

# ###########################################################################
# You can set environment variables here.
# SIMULATION_PROGRAM is the simulation program name. It is already set by the
# environment's simulation.config-stage0!
# EXAMPLE: if the simulation binary is already on all PEs, do not copy
#          simulation-environment.tar.bz2 and set the full path to the
#          simulation binary here!

# SIMULATION_PROGRAM=<Path>/<New Name>
